# Roadmap

## Next

- Replace the placeholder contact endpoint (`mailto:hello@mistermister.no`) with the real contact path (Calendly, form, or preferred email). Ensure all CTAs point to it.
- Re-tone each Mister card to match the "Operational Intelligence" narrative (rewrite `description` + `personality` in `src/data/misters.ts`) and add lightweight proof points where possible (links, screenshots, outcomes).

